-- Shared prelude: sequents `G ⇒ a` over `ctx.mm0`.
import "ctx.mm0";
import "iff.mm0";

term seq (g: ctx) (a: wff): wff;
infixl seq: $⇒$ prec 0;

--| @congr
axiom seq_congr (g h: ctx) (a b: wff):
  $ ctx_eq g h $ > $ a ↔ b $ > $ (g ⇒ a) ↔ (h ⇒ b) $;
